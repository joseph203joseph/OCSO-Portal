'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const[code,setCode]=useState('');const[err,setErr]=useState('');const r=useRouter();async function go(){const x=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code,role:'admin'})});if(x.ok)r.push('/admin');else setErr('Incorrect admin code.')}return <main className="gate"><section className="gatebox"><h1>Eight Legged Customs Admin</h1><p>Authorized management access only.</p><input className="input" type="password" value={code} onChange={e=>setCode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&go()} placeholder="Admin access code"/><button className="btn full" onClick={go}>Open Dashboard</button><div style={{color:'#a20b1c',marginTop:10}}>{err}</div></section></main>}