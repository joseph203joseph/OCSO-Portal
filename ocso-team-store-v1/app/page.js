'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const[code,setCode]=useState('');const[err,setErr]=useState('');const r=useRouter();async function go(){setErr('');const x=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code,role:'store'})});if(x.ok)r.push('/store');else setErr('Incorrect access code.')}return <main className="gate"><section className="gatebox"><h1>Private Department Store</h1><p>Enter the authorized access code to open the Orange County Sheriff’s Office headwear program.</p><input className="input" type="password" value={code} onChange={e=>setCode(e.target.value)} onKeyDown={e=>e.key==='Enter'&&go()} placeholder="Access code"/><button className="btn full" onClick={go}>Enter Store</button><div style={{color:'#a20b1c',marginTop:10}}>{err}</div></section></main>}