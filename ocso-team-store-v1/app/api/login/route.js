import {NextResponse} from 'next/server';import {token} from '../../../lib/auth';export async function POST(req){const{code,role}=await req.json();const ok=role==='admin'?code===process.env.ADMIN_ACCESS_CODE:code===process.env.STORE_ACCESS_CODE;if(!ok)return NextResponse.json({ok:false},{status:401});const r=NextResponse.json({ok:true});r.cookies.set(role==='admin'?'admin_auth':'store_auth',token(role),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',maxAge:60*60*12,path:'/'});return r}